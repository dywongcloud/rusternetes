apiVersion: v1
kind: Service
metadata:
  name: nginx-clusterip
  namespace: test-namespace
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
