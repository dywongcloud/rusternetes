apiVersion: v1
kind: Pod
metadata:
  name: test-nginx
  namespace: test-namespace
  labels:
    app: nginx
    test: basic
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
    - containerPort: 80
      name: http
