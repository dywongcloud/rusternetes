kind: Pod
apiVersion: v1
metadata:
  name: nginx-pod
  namespace: test-namespace
  labels:
    app: nginx
spec:
  containers:
  - name: nginx
    image: nginx:1.25-alpine
    ports:
    - containerPort: 80
      name: http
