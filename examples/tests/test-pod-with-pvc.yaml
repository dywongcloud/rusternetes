apiVersion: v1
kind: Pod
metadata:
  name: test-pod-with-pvc
  namespace: test-namespace
spec:
  containers:
  - name: nginx
    image: nginx:latest
    volumeMounts:
    - name: data
      mountPath: /data
  volumes:
  - name: data
    persistentVolumeClaim:
      claimName: test-pvc
