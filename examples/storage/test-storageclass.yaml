apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: fast-storage
provisioner: rusternetes.io/hostpath
parameters:
  type: local
  path: /tmp/rusternetes-storage
reclaimPolicy: Delete
volumeBindingMode: Immediate
allowVolumeExpansion: true
